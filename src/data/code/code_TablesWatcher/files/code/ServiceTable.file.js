// ServiceTable.js rendered by Table.js
const ServiceTable = ({ tableRef }) => {
  useTableVisibility({
    tableRef, // props's tableRef
    check: "bool",
    watch: "any",
  });

  return (
    <>
      {
        // table returns its children
      }
    </>
  );
};
